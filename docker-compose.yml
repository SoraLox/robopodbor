# Фронт поднимается одной командой и не ждёт бэкенд:
# в сборке включены MSW-моки поверх контракта contracts/openapi.yaml.
services:
  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    ports:
      - "8080:80"
    restart: unless-stopped
